%li
  %ul.pricing-table
    %li.title.text-center
      = render 'shared/control_header', object: breed, text: breed.name

    %li.cta-button
      = link_to 'Cats', admin_cats_path(breed_id: breed.id), class: 'text-breaker radius button'
