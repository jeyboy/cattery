.pad_top English
= f.text_area :en, placeholder: 'English Page body..', rows: 10, class: 'editor'
.pad_top Russian
= f.text_area :ru, placeholder: 'Russian Page body..', rows: 10, class: 'editor'

:javascript
  $('.editor').editable({
      inlineMode: false,
      //paragraphy: false,
      spellcheck: true,
      //toolbarFixed: false
      //imageUploadParam: ,
      //imageUploadURL: '#{cl_upload_url({})}',
      //imageUploadParams: #{cl_upload_tag_params({})}

      imageUploadURL: '/admin/gallery',
      imageUploadParams: #{{authenticity_token: form_authenticity_token}.to_json}
  }).on('editable.imageError', function (e, editor, error) {
      console.log(error);
  });
